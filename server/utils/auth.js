// const jwt = require('jsonwebtoken');

// // Function to generate a JWT token
// const generateToken = (adminId) => {
//     return jwt.sign({ id: adminId }, process.env.KEY || 'your_secret_key', { expiresIn: '1h' });
// };

// module.exports = { generateToken };
